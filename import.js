export const multi = (items) =>items.reduce((acc,item) =>acc*item,0);
export const div = (items) =>items.reduce((acc,item) =>acc/item,0);
export const sous = (items) =>items.reduce((acc,item) =>acc-item,0);
export const add = (items) =>items.reduce((acc,item) =>acc+item,0);
